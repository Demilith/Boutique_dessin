# Frontend (Vue + Vite)

Quickstart

```bash
cd frontend
npm install
npm run dev
```

The frontend expects the backend API at the same origin; for development, you can run the Django dev server on port 5173 or configure a proxy in Vite.
